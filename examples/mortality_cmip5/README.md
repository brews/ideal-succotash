# mortality_cmip5

The various clean_*.py scripts are run as setup, for data cleaning, to create input data for the example analysis.

This was run within a `pangeo/pangeo-notebook:2024.11.11` container. Analysis notebooks were run on https://notebooks.cilresearch.org.
